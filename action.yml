# action.yml
name: 'TRIVY SCAN'
description: 'SCAN CONTAINER IMAGES WITH TRIVY'
inputs:
    image-name:
         dexcription: name of image to be scanned
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: aquasec/trivy
  args:
    - ${{ inputs.image-name }}
